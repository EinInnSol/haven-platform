{
  "name": "haven-platform",
  "version": "1.0.0",
  "description": "AI-powered homeless services platform",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute haven_db --file=./src/database/schema.sql",
    "db:seed": "wrangler d1 execute haven_db --file=./src/database/seed.sql"
  },
  "dependencies": {
    "itty-router": "^4.0.0",
    "itty-cors": "^1.0.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}